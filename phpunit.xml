<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
        <testsuite name="Browser">
            <directory>tests/Browser</directory>
        </testsuite>
        <testsuite name="Arch">
            <file>tests/Arch.php</file>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
        <!-- Blank rather than absent: bootstrap/app.php reports every exception to Sentry
             (#433), so a developer's real SENTRY_DSN would send deliberately thrown test
             failures to the production project. An empty DSN disables the SDK outright
             instead of erroring at boot.

             Both lines are required, and neither is redundant. An exported SENTRY_DSN
             reaches PHP through three channels: getenv(), $_ENV and $_SERVER. The forced
             <env> blanks the first two but leaves $_SERVER intact, and Laravel's Env
             repository reads $_SERVER — so without the <server> line an exported DSN still
             wins and the suite still transmits. Verified by probing all three sources.

             force="true" on <env> is likewise load-bearing: without it PHPUnit leaves an
             already-exported value untouched. Reading .env is not the only way the
             variable arrives — a shell, CI job or direnv profile can export it. -->
        <env name="SENTRY_DSN" value="" force="true"/>
        <server name="SENTRY_DSN" value=""/>
    </php>
</phpunit>
